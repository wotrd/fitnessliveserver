<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.hospital.mapper.CaseinfoMapper">

    <resultMap type="com.example.hospital.domain.Caseinfo" id="CaseinfoMap">
        <result property="id" column="id" jdbcType="BIGINT"/>
        <result property="orderUser" column="order_user" jdbcType="VARCHAR"/>
        <result property="doctorName" column="doctor_name" jdbcType="VARCHAR"/>
        <result property="departName" column="depart_name" jdbcType="VARCHAR"/>
        <result property="bingQingZhen" column="bing_qing_zhen" jdbcType="VARCHAR"/>
        <result property="chuFang" column="chu_fang" jdbcType="VARCHAR"/>
        <result property="dealTime" column="deal_time" jdbcType="TIMESTAMP"/>
    </resultMap>

    <!--查询单个-->
    <select id="queryById" resultMap="CaseinfoMap">
        select
          id, order_user, bing_qing_zhen, chu_fang, deal_time
        from hospital.caseinfo
        where id = #{id}
    </select>
    <!--查询单个-->
    <select id="queryByBqz" resultMap="CaseinfoMap">
        select
          id, order_user, bing_qing_zhen, chu_fang, deal_time,depart_name,doctor_name
        from hospital.caseinfo
        where bing_qing_zhen = #{bqz}
    </select>


    <!--查询指定行数据-->
    <select id="queryAll" resultMap="CaseinfoMap">
        select
          id, order_user, bing_qing_zhen, chu_fang,doctor_name,depart_name, deal_time
        from hospital.caseinfo
         where order_user = #{account}
         and (bing_qing_zhen like concat('%', #{reason}, '%')
         or chu_fang like concat('%', #{reason}, '%')
         or depart_name like concat('%', #{reason}, '%')
         or doctor_name like concat('%', #{reason}, '%'))

         order by id desc
    </select>


    <!--新增所有列-->
    <insert id="insert" keyProperty="id" useGeneratedKeys="true">
        insert into hospital.caseinfo(order_user, bing_qing_zhen, chu_fang, deal_time)
        values (#{orderUser}, #{bingQingZhen}, #{chuFang}, #{dealTime})
    </insert>

    <!--通过主键修改数据-->
    <update id="update">
        update hospital.caseinfo
        <set>
            <if test="orderUser != null and orderUser != ''">
                order_user = #{orderUser},
            </if>
            <if test="bingQingZhen != null and bingQingZhen != ''">
                bing_qing_zhen = #{bingQingZhen},
            </if>
            <if test="chuFang != null and chuFang != ''">
                chu_fang = #{chuFang},
            </if>
            <if test="dealTime != null">
                deal_time = #{dealTime},
            </if>
        </set>
        where id = #{id}
    </update>

    <!--通过主键删除-->
    <delete id="deleteById">
        delete from hospital.caseinfo where id = #{id}
    </delete>

</mapper>