<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.sceneserver.manager.mapper.ComplaintMapper">

    <resultMap type="com.example.sceneserver.manager.po.ComplaintPo" id="ComplaintMap">
        <result property="id" column="id" jdbcType="BIGINT"/>
        <result property="name" column="name" jdbcType="VARCHAR"/>
        <result property="reason" column="reason" jdbcType="VARCHAR"/>
        <result property="createTime" column="create_time" jdbcType="TIMESTAMP"/>
    </resultMap>

    <select id="queryLikeName" resultMap="ComplaintMap">
        select
          id, name, reason, create_time
        from complaint
        where name like concat('%', #{name}, '%')
        order by id desc
    </select>


    <!--新增所有列-->
    <insert id="insert" keyProperty="id" useGeneratedKeys="true">
        insert into complaint(name, reason, create_time)
        values (#{name}, #{reason}, now())
    </insert>

    <delete id="deleteByIds">
        delete from complaint
        where id in
        <foreach collection="ids" item="id" separator="," open="(" close=")">
            #{id}
        </foreach>
    </delete>

</mapper>